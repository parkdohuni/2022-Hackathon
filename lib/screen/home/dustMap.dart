import 'package:flutter/cupertino.dart';

class DustMap extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("dustMap Screen"),
      )
    );
  }
}