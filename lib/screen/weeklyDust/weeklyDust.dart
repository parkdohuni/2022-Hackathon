import 'package:flutter/cupertino.dart';

class WeeklyDust extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
          child: Text("NoKidsZoneCafe Screen"),
        )
    );
  }
}